# Copy this file to server/.env and fill in your own values.

PORT=4000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change-me
DB_NAME=boodschapgemak

# Shared secret both phones send with every request.
# Any long random string is fine - just use the same one in the app.
HOUSEHOLD_KEY=change-me-to-something-long-and-random
